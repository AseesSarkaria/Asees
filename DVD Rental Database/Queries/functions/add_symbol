CREATE OR REPLACE FUNCTION add_symbol(value NUMERIC, column_name TEXT)
RETURNS TEXT AS 
$$
BEGIN
    RETURN 
		CASE 
			WHEN column_name LIKE '%_p' THEN CONCAT(value, '%')
			WHEN column_name LIKE '%_m' THEN CONCAT('$', value)
			--other cases
			ELSE value::TEXT
		END;
END
$$ 
LANGUAGE plpgsql
